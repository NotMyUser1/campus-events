# Campus-Events
## starten
1. MongoDB installieren und starten
2. in das Verzeichnis server navigieren
3. `npm install` ausführen
4. `node run dev` ausführen. In der Console sollte "Connected to MongoDB" stehen! Sonst stimmt etwas mit MongoDB nicht (z.B. falscher Port)
5. in das Verzeichnis my-react-app navigieren
6. `npm install` ausführen
7. `npm run dev` ausführen
